const reqString = {
  type: String,
  required: true
}

const reqNumber = {
  type: Number,
  required: true
}

const reqArray = {
  type: Array,
  required: true
}

module.exports = {
  reqString,
  reqNumber,
  reqArray
}